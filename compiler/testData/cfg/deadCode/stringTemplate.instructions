== test ==
fun test() {
    "${throw Exception()} ${1}"
}
---------------------
L0:
  0 <START>
  1 mark({ "${throw Exception()} ${1}" })
  1 mark("${throw Exception()} ${1}")
  1 mark(throw Exception())
  1 mark(Exception())
  1 call(Exception, <init>)
  1 throw (throw Exception())                NEXT:[<ERROR>]
- 1 r(1)                                     PREV:[]
- 1 r("${throw Exception()} ${1}")           PREV:[]
L1:
  0 <END>                                    NEXT:[<SINK>]                                   PREV:[]
error:
  0 <ERROR>                                  PREV:[throw (throw Exception())]
sink:
  0 <SINK>                                   PREV:[<ERROR>, <END>]
=====================

== foo ==
fun foo(a: Array<Int>) {
    a[0] += 1
}
---------------------
L0:
  0 <START>
  0 v(a: Array<Int>)
  0 w(a)
  1 mark({ a[0] += 1 })
  1 mark(a[0] += 1)
  1 mark(a[0])
  1 r(a)
  1 r(0)
  1 call(a[0], get)
  1 r(1)
  1 call(+=, plus)
  1 mark(a[0])
  1 r(a)
  1 r(0)
  1 r(1)
  1 call(a[0], set)
L1:
  0 <END>                  NEXT:[<SINK>]
error:
  0 <ERROR>                PREV:[]
sink:
  0 <SINK>                 PREV:[<ERROR>, <END>]
=====================

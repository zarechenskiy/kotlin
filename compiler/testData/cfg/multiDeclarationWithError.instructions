== foo ==
fun foo(x: Int) {
    val (a, b) = x
    a
}
---------------------
L0:
  0 <START>
  0 v(x: Int)
  0 w(x)
  1 mark({ val (a, b) = x a })
  1 r(x)
  1 v(a)
  1 w(a)
  1 v(b)
  1 w(b)
  1 r(a)
L1:
  0 <END>                         NEXT:[<SINK>]
error:
  0 <ERROR>                       PREV:[]
sink:
  0 <SINK>                        PREV:[<ERROR>, <END>]
=====================
